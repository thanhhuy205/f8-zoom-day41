const NAME_SPACES = "product";

const GET_LIST = `${NAME_SPACES}/get-list`;
const SET_LIST = `${NAME_SPACES}/set-list`;
const GET_DETAIL = `${NAME_SPACES}/get-detail`;
const SET_DETAIL = `${NAME_SPACES}/set-detail`;

export { GET_DETAIL, GET_LIST, SET_DETAIL, SET_LIST };
